// What is the starting index of an array?
// = 0

// What would be the index of “developer” in the array?

// var dream = [“I”, “will”, “become”, “a”, “developer”, “inshallah”];
// = 4

// How will you get the second element of the array arr = [3, 7, 10]?
// = arr.indexof(1)

// Which method will you use to add an element to an array?
// = .push()

// Which method will you use to remove the first element from an array? (if you haven’t google it before, now google it :D)

// = .shift()

// What would nums.indexOf(0) and nums.indexOf(1) return respectively, if

// var nums = [1, 5, 8]?
// = -1,0;

// What will be the output console.log(6 != 6);
// = false

// What will be the output console.log(6 >= 6);
// =true

// Where you don’t have to add a condition?
// = else

// What will you write to fulfill both conditions below?

// if(cokePrice < 15 ____?____ isHot == true);
// = &&
